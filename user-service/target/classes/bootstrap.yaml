spring:
  application:
    name:
      - userservice #服务名称
  profiles:
    active:
      - dev #开发环境
#      - test
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml